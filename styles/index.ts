import { createStyles, makeStyles } from "@mui/styles"

const useStyles = makeStyles(() =>
  createStyles({
    title: {
      fontWeight: 550,
      fontSize: 14,
      color: "red",
    },
  })
)

export default useStyles
